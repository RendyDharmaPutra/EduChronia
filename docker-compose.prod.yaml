services:
  web:
    container_name: educhronia-web
    build:
      context: ./EduChronia-web
      dockerfile: Dockerfile.prod
      args:
        - NUXT_PUBLIC_APP_URL=${HOST_WEB_BASE_URL}
        - NUXT_PUBLIC_HOST_API_BASE_URL=${HOST_API_BASE_URL}
    ports:
      - "${WEB_PORT}:3000"
    environment:
      - APP_VERSION=${APP_VERSION}
      - NODE_ENV=production
      - NUXT_PUBLIC_APP_URL=${HOST_WEB_BASE_URL}
      - NUXT_PUBLIC_HOST_API_BASE_URL=${HOST_API_BASE_URL}
    depends_on:
      - api
    restart: unless-stopped

  api:
    container_name: educhronia-api
    build:
      context: ./EduChronia-api
      dockerfile: Dockerfile.prod
      target: runner
    ports:
      - "${API_PORT}:${API_PORT}"
    environment:
      - APP_VERSION=${APP_VERSION}
      - NODE_ENV=production
      - PORT=${API_PORT}
      - HOST=${API_HOST}
      - HOST_WEB_BASE_URL=${HOST_WEB_BASE_URL}
      - HOST_API_BASE_URL=${HOST_API_BASE_URL}
      - BETTER_AUTH_BASE_URL=${BETTER_AUTH_BASE_URL}
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:${POSTGRES_PORT}/${POSTGRES_DB}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  db-migration:
    container_name: educhronia-db-migration
    build:
      context: ./EduChronia-api
      dockerfile: Dockerfile.prod
      target: migrator
    environment:
      - DB_URL=${POSTGRES_URL}
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"

  postgres:
    image: postgres:18-alpine
    container_name: educhronia-postgres
    ports:
      - "${POSTGRES_HOST_PORT}:${POSTGRES_PORT}"
    environment:
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB} -h localhost",
        ]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  pgdata:
    name: educhronia_pgdata
